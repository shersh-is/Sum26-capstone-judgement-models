services:

  api:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  streamlit:
    build: .
    command: streamlit run app_demo.py --server.address=0.0.0.0 --server.port=8501
    ports:
      - "8501:8501"